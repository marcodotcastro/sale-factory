<div class="row">
  <div class="col-md-12 col-sm-12 col-xs-12">
    <div class="x_panel">
      <div class="x_title">
        <h2>Meu Perfil
          <small>...</small>
        </h2>
        <ul class="nav navbar-right panel_toolbox">
          <li><a class="collapse-link"><i class="fa fa-chevron-up"></i></a>
          </li>
        </ul>
        <div class="clearfix"></div>
      </div>
      <div class="nav navbar-right">
        <%#TODO: Mover para o helpers %>
        <% if current_usuario.representante? %>
          <%= link_to 'Editar Perfil', edit_representante_path(@representante), class: "btn btn-primary" %>
          <%= link_to 'Excluir Perfil', @representante, method: :delete, class: "btn btn-danger", data: {confirm: 'Você tem certeza?'} %>
        <% end %>
      </div>
      <%= render 'representante' %>
      <div class="x_content">
        <div class="divider-dashed"></div>
        <form data-parsley-validate class="form-horizontal form-label-left">
          <b>Medalhas <%= link_to "(regras)", representante_jogos_path(current_usuario.representante) %>: </b>
          <% current_usuario.badges.each do |bagde| %>
            <%= bagde.name %>,
          <% end %>
        </form>
      </div>
    </div>
  </div>
</div>

